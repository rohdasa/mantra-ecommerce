// import { Navigate } from "react-router-dom";
// import useAuthStore from "../store/authStore";

// const RequireAuth = ({ children }) => {
//   // Get the isAuthenticated status from your global state store
//   const { isAuthenticated } = useAuthStore();

//   // If the user is not authenticated, redirect them to the home page
//   // and trigger the login modal to open.
//   if (!isAuthenticated) {
//     return <Navigate to="/" />;
//   }

//   // If the user is authenticated, render the child component (WishlistPage)
//   return children;
// };

// export default RequireAuth;
